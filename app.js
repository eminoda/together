const { Application } = require('./main');
const getReady = require('get-ready');

// const app = new Application();
// app.use(async ctx => {
// 	ctx.body = 'hello together';
// });

// getReady.mixin(app);

// app.ready(() => {
// 	app.listen(3111);
// });

// app.ready(true);
